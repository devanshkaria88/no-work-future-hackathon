services:
  postgres:
    image: postgres:16-alpine
    container_name: borough-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: borough
    ports:
      - "5432:5432"
    volumes:
      - borough_data:/var/lib/postgresql/data

volumes:
  borough_data:
